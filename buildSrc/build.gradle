apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.tools.build:gradle:2.1.0'
    compile gradleApi()
    compile localGroovy()
}
